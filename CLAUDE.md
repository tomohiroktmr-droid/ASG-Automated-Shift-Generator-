# プッチョン シフト管理スクリプト — 開発方針

## プロジェクト概要
Google スプレッドシート + Google Apps Script (GAS) で動作する
飲食店「プッチョン」向けシフト管理ツール。

## 現在のフォーカス（2026年4月時点）
**共有カレンダーからのシフト収集機能** に特化して開発・改善する。

### 優先課題
- 共有カレンダーのイベントを正確に各スタッフへ紐づけること
- 重複データの防止

### 休止中の機能
- **AI シフト最適化機能**（`Optimizer.gs`）は一旦開発を休止。
  コードは保持するが、積極的な改修は行わない。

---

## 重要な実装ルール

### イベントとスタッフ名の照合（必須）
共有カレンダー運用（全員が同一カレンダー ID に書き込む）のため、
`getEventsForStaff()` では **イベントタイトルにスタッフ名が含まれる場合のみ** そのスタッフの予定として記録する。

```javascript
// この行は削除・変更禁止
if (title.indexOf(staff.name) === -1) return;
```

この照合を外すと全スタッフ分のイベントが重複して出力される。

### スタッフ管理シートの列構成
| 列 | 内容 |
|----|------|
| A | スタッフ名（イベントタイトルの照合キー） |
| B | 共有カレンダー ID（全スタッフ同じ値） |
| C | 役職（社長 / 店長 / キッチンA / キッチンB / スタッフ） |

---

## ファイル構成
| ファイル | 役割 |
|---------|------|
| `Code.gs` | メイン処理（シフト収集・集計・シート出力） |
| `Optimizer.gs` | AI シフト最適化エンジン（現在休止中） |

---

## 開発ブランチ
`claude/calendar-to-spreadsheet-Mj4mn`
